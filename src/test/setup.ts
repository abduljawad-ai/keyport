// Vitest global setup: jest-dom matchers + Supabase client env so the
// shared client module can be imported in tests (the client itself is
// mocked per-test).
import "@testing-library/jest-dom/vitest";
